The application here that is build using python flask is a PDF Splitter. It extracts pages from the uploaded PDF in the specified page rage. 
The front end for this application which is a Flask service accepting input from the user through the webbrowser. The front end is python flask service with Jinja2 templating. Flask is a micro web framework written in Python. It is classified as a microframework because it does not require particular tools or libraries. It has no database abstraction layer, form validation, or any other components where pre-existing third-party libraries provide common functions. Jinja2 templating is used to interact with the HTML templates and python code.
The backend is written in PyPDF2. PyPDF2 is a python library built as a PDF toolkit. It is capable of: Extracting document information (title, author, …), Splitting documents page by page, Merging documents page by page, Cropping pages, Merging multiple pages into a single page, Encrypting and decrypting PDF files, and more!
The demo of our pdf-splitter application is available at https://mittal.pythonanywhere.com/
Finally we have deployed our application on pythonanywhere.com which is a great free cloud hosting service for python. The hosting provider that we have used here is pythonanywhere.com which is an online integrated development environment and web hosting service based on the Python programming language. It provides in-browser access to server-based Python and Bash command-line interfaces, along with a code editor with syntax highlighting.